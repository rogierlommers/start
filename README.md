# start

A go backend for a home dashboard: serves HTML and APIs for login, bookmarks, events, storage/uploads, and stats with Gin + SQLite. It also sends quick notes by email (text/files) and includes a URL collector that scrapes titles and publishes a personal RSS feed.
